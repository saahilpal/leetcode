# Two Sum

**Difficulty:** Easy  
**Pattern:** Arrays & Hashing  
**Language:** cpp  
**LeetCode:** https://leetcode.com/problems/two-sum/description/

---

## Intuition
_Write your first thoughts and observations here._

---

## Approach
_Explain the algorithm step by step._

---

## Example Walkthrough
_Walk through a small example to show how the logic works._

---

## Time Complexity
_O(?)_

---

## Space Complexity
_O(?)_

---

## Code

```cpp
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int, int> mp;
        vector<int> res;
        for (int i = 0; i < nums.size(); i++) {
            int need = target - nums[i];
            if (mp.find(need) != mp.end()) {
                int j=mp[need];
                res.push_back(i);
                res.push_back(j);
                break;
            }
            mp[nums[i]] = i;
        }
        return res;
    }
};
```